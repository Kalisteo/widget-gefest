import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import Widget from "./Widget/Widget";

const root = ReactDOM.createRoot(document.getElementById('gefest'));
root.render(
    <Widget/>
);
